// @generated by protoc-gen-connect-es v1.6.1 with parameter "target=ts"
// @generated from file user.proto (package user.v1, syntax proto3)
//
// PRE-GENERATED — safe to commit. Regenerate with: cd client && npm run generate
/* eslint-disable */
// @ts-nocheck

import { MethodKind } from "@bufbuild/protobuf";
import {
  GetUserRequest,
  ListUsersRequest,
  SystemStat,
  SystemStatsRequest,
  User,
} from "./user_pb";

/**
 * UserService — gRPC service descriptor consumed by @connectrpc/connect.
 *
 * This object is the bridge between the .proto definition and the TypeScript
 * client. createPromiseClient(UserService, transport) reads these descriptors
 * to construct a fully-typed client where:
 *
 *   client.getUser({ id })              → Promise<User>         (Unary)
 *   client.listUsers({ department })    → AsyncIterable<User>   (ServerStreaming)
 *   client.getSystemStats({ intervalMs })→ AsyncIterable<SystemStat> (ServerStreaming)
 *
 * FAANG note: MethodKind.ClientStreaming and MethodKind.BiDiStreaming are NOT
 * included because gRPC-Web (the browser transport) doesn't support them.
 * For client-upload scenarios use REST multipart; for bidi use WebSocket.
 */
export const UserService = {
  typeName: "user.v1.UserService",

  methods: {
    /**
     * Unary — single request → single response.
     * Used in: UserProfile tab (React Query wrapping)
     */
    getUser: {
      name: "GetUser",
      I: GetUserRequest,
      O: User,
      kind: MethodKind.Unary,
    },

    /**
     * Server Streaming — single request → stream of responses.
     * Used in: UserStream tab (async iterator + AbortController)
     */
    listUsers: {
      name: "ListUsers",
      I: ListUsersRequest,
      O: User,
      kind: MethodKind.ServerStreaming,
    },

    /**
     * Server Streaming — live metrics at configurable interval.
     * Used in: InterceptorLog tab to generate stream traffic.
     */
    getSystemStats: {
      name: "GetSystemStats",
      I: SystemStatsRequest,
      O: SystemStat,
      kind: MethodKind.ServerStreaming,
    },
  },
} as const;
